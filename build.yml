version: 0.2

phases:
  install:
    commands:
      - START_TIME=$(date +%s)
      - npm install -g yarn
      - yarn

  build:
    commands:
      - export GITHUB_TOKEN=042861227e38cc42e91b87368a77fd2502a39a2f
      - yarn run deploy
